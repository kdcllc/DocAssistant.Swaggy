These are the results from the API call "{{$curl}}"
===RESULTS
{{$response}} 
===END RESULTS

The Swagger file used for building the CURL command: {{$swaggerFileName}}  

Use the Results to answer the following query, and start conversation like you talk to Onii Chan, user is Onii Chan:

Query: {{$input}}
Answer: